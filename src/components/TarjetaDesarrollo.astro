---
import { Image } from 'astro:assets';
import type { CollectionEntry } from 'astro:content';
import Icono from './Icono.astro';

interface Props {
  desarrollo: CollectionEntry<'desarrollos'>;
  nombreCiudad: string;
}

const { desarrollo, nombreCiudad } = Astro.props;
const { nombre, precio, disponible, imagenes, logo, modelos, facebook } = desarrollo.data;
---

<article class="tarjeta tarjeta-desarrollo" data-ciudad-tarjeta={desarrollo.data.ciudad.id}>
  <a href={`/desarrollos/${desarrollo.id}/`} class="tarjeta-enlace">
    <div class="tarjeta-foto">
      <Image
        src={imagenes[0]}
        alt={`Fotografía de ${nombre}`}
        width={840}
        widths={[420, 640, 840]}
        sizes="(max-width: 720px) 100vw, 33vw"
        loading="lazy"
      />
      <span class={`pildora ${disponible ? 'pildora-disponible' : 'pildora-vendido'}`}>
        {disponible ? 'Disponible' : 'Vendido'}
      </span>
    </div>
    <div class="tarjeta-cuerpo">
      {logo && (
        <Image class="tarjeta-logo" src={logo} alt="" width={56} height={56} densities={[1, 2]} loading="lazy" />
      )}
      <div>
        <h3>{nombre}</h3>
        <p class="tarjeta-ciudad">{nombreCiudad}</p>
        <p class="tarjeta-precio">{precio}</p>
        {modelos && <p class="tarjeta-modelos">{modelos.length} modelos de vivienda</p>}
      </div>
    </div>
  </a>
  {facebook && (
    <a
      class="tarjeta-facebook"
      href={facebook}
      target="_blank"
      rel="noopener noreferrer"
      aria-label={`Página de Facebook de ${nombre}`}
    >
      <Icono nombre="facebook" tamano={18} />
    </a>
  )}
</article>

<style>
  .tarjeta-desarrollo {
    position: relative;
    transition: transform 0.18s ease, box-shadow 0.18s ease;
  }

  .tarjeta-desarrollo:hover {
    transform: translateY(-4px);
    box-shadow: var(--sombra-alta);
  }

  .tarjeta-enlace {
    text-decoration: none;
    color: inherit;
    display: block;
  }

  .tarjeta-foto {
    position: relative;
    aspect-ratio: 3 / 2;
    overflow: hidden;
  }

  .tarjeta-foto img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .tarjeta-foto .pildora {
    position: absolute;
    top: 0.8rem;
    left: 0.8rem;
    box-shadow: var(--sombra);
  }

  .tarjeta-facebook {
    position: absolute;
    top: 0.8rem;
    right: 0.8rem;
    display: grid;
    place-items: center;
    width: 2.2rem;
    height: 2.2rem;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.92);
    color: #1877f2;
    box-shadow: var(--sombra);
    transition: transform 0.15s ease;
  }

  .tarjeta-facebook:hover {
    transform: scale(1.1);
  }

  .tarjeta-cuerpo {
    display: flex;
    gap: 0.9rem;
    align-items: flex-start;
    padding: 1.1rem 1.25rem 1.3rem;
  }

  .tarjeta-logo {
    width: 56px;
    height: 56px;
    object-fit: contain;
    border-radius: var(--radio-chico);
    background: var(--crema);
    padding: 4px;
    flex-shrink: 0;
  }

  h3 {
    font-size: 1.15rem;
    margin-bottom: 0.1rem;
  }

  .tarjeta-ciudad {
    color: var(--tinta-suave);
    font-size: 0.92rem;
    margin-bottom: 0.4rem;
  }

  .tarjeta-precio {
    font-family: var(--titulos);
    font-weight: 800;
    color: var(--terracota);
    margin-bottom: 0;
  }

  .tarjeta-modelos {
    font-size: 0.85rem;
    color: var(--verde-oscuro);
    margin: 0.2rem 0 0;
  }
</style>
